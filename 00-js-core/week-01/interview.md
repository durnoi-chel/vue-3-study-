Почему const obj = {} позволяет менять свойства объекта, хотя переменная объявлена как константа? В чём разница между изменением ссылки и изменением значения?
Потому что мы можем представить что obj это ярлык на коробке и const недаёт нам менять его так скажем отрывать от одной коробки и вешать на другую, а значение это то что лежит в коробке и =его мы можем менять 

всплытие это механизм в js при котором обявленные функции и переменные как бы поднимаются наверх создаётся впечатление что это происходит физически но на самом деле происходит регистрация в лексическом окружении во время компеляции но для разных типов работает по разному для var: объявление как бы поднимается в начало но инициализация происходит в месте фактического объявления до этого undefined 
let  и const: вообще не инициализируются до фактического объявления они находятся в NDZ и попытка обратиться к ней вызовет ошибку
function declaration: уПолностью поднимается наверх в начало области видимости

в обычной функции значение this определяется динамически оно зависит от контекста вызова 
при вызове метода как объекта он ссылвется на этот объект 

стрелочная же функция не имеет собственного контеста thus они наслудуют его из внешней области видимости
стрелочную функцию нельзя использовать  в случаях методов с динамическим this если метод должен с разными объектами 
Event listeners с динамическим this в обработчиках событий this обычно ссылается на элемент, который вызвал событие стрелочная функция «потеряет» эту ссылку
Методы, использующие arguments или caller Стрелочные функции не имеют доступа к arguments и caller
Контекстно-зависимые методы Если функция должна менять поведение в зависимости от объекта, к которому применяется

историческая проблемма что typeof null выводит строку object связана с тем что используется тег типа и как раз для объектов использовался 000 а null представлялся как 0х00 в следствии чего выводится именно ээтот ответ при попытке исправить было предложение заменить функцию на type() но они были не совместимы с typeof что привело бы к большим крашам многих сайтов

console.log(1 + "2" + "2"); выведет 122 так как сработает встроеннный метод ToString и произайдёт конкатинация
console.log(1 + +"2" + "2"); выведет 32 так как + сработает как метод ToInt что достаточно часто используется для перевода из строки 
console.log(1 + -"1" + "2");выведет 02 так как - превратит из строки в число и применит инверсию то есть знак -
console.log(+""); выедет 0 потому что унарный плюс пытается преобразовать значение в числовой формат. По скольку пустая строчка то он преобразует её в 0